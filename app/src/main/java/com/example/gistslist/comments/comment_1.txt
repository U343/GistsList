-- Добавить отображение ошибки в случае фейла загрузки
-- Добавить отображение прогресс бара, когда происходит загрузка
-- Файлы неотформатированы, из-за этого код выглядит некрасиво))0)

-- BaseGist
    //TODO как выяснилось если просто не указыать поля, то retrofit спокойно работает и заполняет...
    Да, если нет нужды указывать все параметры, то можно не указывать. Это зависит от специфики
    проекта, задачи и общепринятого кодстайла в коде
    //TODO Оставить в BaseGist и Owner только нужны поля, которые используеются

-- urls.kt
    Урлы, пути, параметры должны лежать там, где они используются, просто так хранить их висящими
    коснтантами плохо, так как раскрывается условная реализация
    //TODO Перенести константы к местам использования

-- ItemListAdapter
    Все так же каждый раз создается новый инстанс адаптера
    //TODO Поправить логику с созданием нового инстанса адаптера

-- MainActivity
    //TODO красиво ли объявление вьюшек и их инициальзацию оставлять в MainActivity?...
    Если активити отвечает за представление на конкретном экране и содержит в себе вьюшки, то да,
    нормально

    clickAddButton() -- лишний метод, который не дает никакого профита

-- MainListViewModel
    Обсудим на созвоне что тут не так и что надо переделать